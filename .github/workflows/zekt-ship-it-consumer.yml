name: "Ship It - Deploy Page"

on:
  repository_dispatch:
    types: [ship-it-demo]

jobs:
  deploy-page:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # Spec 119, 4.8 — mint the OIDC token the Zekt demo API requires
      contents: write  # the Deploy step commits and pushes to trigger GitHub Pages
    steps:
      # Spec 119, 4.8 — the Zekt demo API authenticates progress reports with the GitHub
      # Actions OIDC token. It used to accept any caller quoting a correlation id, and that id
      # is public: it is echoed below, shown in this run's inputs, and printed on the deployed
      # page. The OIDC token is minted per run and masked immediately — never let it reach a
      # log, or this is back where it started.
      - name: Mint Zekt OIDC token
        shell: bash
        run: |
          RESPONSE=$(curl -sSL \
            -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=api://zekt" || echo '')
          TOKEN=$(echo "$RESPONSE" | jq -r '.value // empty' 2>/dev/null || echo '')
          if [ -z "$TOKEN" ]; then
            echo "::warning::Could not mint a Zekt OIDC token; progress reports will be unauthenticated."
          else
            echo "::add-mask::$TOKEN"
            echo "ZEKT_OIDC_TOKEN=$TOKEN" >> "$GITHUB_ENV"
            echo "🔑 Zekt OIDC token minted"
          fi


      # ── Step 1: Receive event ─────────────────────────────────────────
      - name: "Step 1: Receive Zekt event"
        id: init
        run: |
          CORRELATION="${{ github.event.client_payload.zekt.payload.correlationId }}"
          echo "correlation=$CORRELATION" >> "$GITHUB_OUTPUT"
          echo "📨 Received page from provider"
          echo "   Correlation: $CORRELATION"

          DEMO_API="${{ vars.ZEKT_DEMO_API }}"
          echo "📡 ZEKT_DEMO_API = '${DEMO_API:-<NOT SET>}'"
          if [ -z "$DEMO_API" ]; then
            echo "::warning::ZEKT_DEMO_API variable is not set! Progress reporting will not work."
          else
            HTTP_CODE=$(curl -s -o /tmp/curl_response.txt -w "%{http_code}" -X POST "${DEMO_API}/api/demo/progress" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${ZEKT_OIDC_TOKEN}" \
              -d "{
                \"correlationId\": \"$CORRELATION\",
                \"workflow\": \"consumer\",
                \"step\": 1,
                \"totalSteps\": 4,
                \"status\": \"in_progress\",
                \"message\": \"Event received from provider\",
                \"runId\": ${{ github.run_id }},
                \"runUrl\": \"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
              }") || HTTP_CODE="000"
            echo "📡 Progress report: HTTP $HTTP_CODE"
            cat /tmp/curl_response.txt 2>/dev/null && echo
            if [ "$HTTP_CODE" != "200" ]; then
              echo "::warning::Progress report failed with HTTP $HTTP_CODE"
            fi
          fi

      # ── Step 2: Checkout & write page ─────────────────────────────────
      - name: "Checkout repo"
        uses: actions/checkout@v4

      - name: "Step 2: Write page file"
        run: |
          CORRELATION="${{ steps.init.outputs.correlation }}"
          HTML_B64="${{ github.event.client_payload.zekt.payload.html_b64 }}"

          # Create directory for this demo
          DEPLOY_DIR="docs/demo-${CORRELATION}"
          mkdir -p "$DEPLOY_DIR"

          # Decode and write HTML
          echo "$HTML_B64" | base64 -d > "$DEPLOY_DIR/index.html"

          FILE_SIZE=$(wc -c < "$DEPLOY_DIR/index.html")
          echo "✅ Written to $DEPLOY_DIR/index.html ($FILE_SIZE bytes)"

          DEMO_API="${{ vars.ZEKT_DEMO_API }}"
          if [ -z "$DEMO_API" ]; then
            echo "::warning::ZEKT_DEMO_API not set — skipping progress report"
          else
            HTTP_CODE=$(curl -s -o /tmp/curl_response.txt -w "%{http_code}" -X POST "${DEMO_API}/api/demo/progress" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${ZEKT_OIDC_TOKEN}" \
              -d "{
                \"correlationId\": \"$CORRELATION\",
                \"workflow\": \"consumer\",
                \"step\": 2,
                \"totalSteps\": 4,
                \"status\": \"in_progress\",
                \"message\": \"Page written to repo ✅\"
              }") || HTTP_CODE="000"
            echo "📡 Progress report: HTTP $HTTP_CODE"
            cat /tmp/curl_response.txt 2>/dev/null && echo
            if [ "$HTTP_CODE" != "200" ]; then
              echo "::warning::Progress report failed with HTTP $HTTP_CODE"
            fi
          fi

      # ── Step 3: Commit & push (triggers GitHub Pages) ─────────────────
      - name: "Step 3: Deploy to GitHub Pages"
        run: |
          CORRELATION="${{ steps.init.outputs.correlation }}"

          git config user.name "zekt-deploy-bot"
          git config user.email "deploy@zekt.io"
          git add docs/
          git commit -m "🚀 Deploy demo page: $CORRELATION"
          git push

          echo "✅ Committed and pushed — GitHub Pages will rebuild"

          DEMO_API="${{ vars.ZEKT_DEMO_API }}"
          if [ -z "$DEMO_API" ]; then
            echo "::warning::ZEKT_DEMO_API not set — skipping progress report"
          else
            HTTP_CODE=$(curl -s -o /tmp/curl_response.txt -w "%{http_code}" -X POST "${DEMO_API}/api/demo/progress" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${ZEKT_OIDC_TOKEN}" \
              -d "{
                \"correlationId\": \"$CORRELATION\",
                \"workflow\": \"consumer\",
                \"step\": 3,
                \"totalSteps\": 4,
                \"status\": \"in_progress\",
                \"message\": \"Deployed to GitHub Pages ✅\"
              }") || HTTP_CODE="000"
            echo "📡 Progress report: HTTP $HTTP_CODE"
            cat /tmp/curl_response.txt 2>/dev/null && echo
            if [ "$HTTP_CODE" != "200" ]; then
              echo "::warning::Progress report failed with HTTP $HTTP_CODE"
            fi
          fi

      # ── Step 4: Verify live & report URL ──────────────────────────────
      - name: "Step 4: Verify & report URL"
        run: |
          CORRELATION="${{ steps.init.outputs.correlation }}"
          DEPLOYED_URL="https://zekt-dev-org.github.io/zekt-demo-consumer/demo-${CORRELATION}/"

          # Wait for Pages to propagate
          echo "⏳ Waiting for GitHub Pages propagation..."
          sleep 8

          # Verify page is accessible (retry up to 3 times)
          for i in 1 2 3; do
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$DEPLOYED_URL" || echo "000")
            echo "📡 Attempt $i: HTTP $HTTP_STATUS"
            if [ "$HTTP_STATUS" = "200" ]; then
              break
            fi
            sleep 5
          done

          # Report completion with deployed URL
          DEMO_API="${{ vars.ZEKT_DEMO_API }}"
          if [ -z "$DEMO_API" ]; then
            echo "::warning::ZEKT_DEMO_API not set — skipping final progress report"
          else
            HTTP_CODE=$(curl -s -o /tmp/curl_response.txt -w "%{http_code}" -X POST "${DEMO_API}/api/demo/progress" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${ZEKT_OIDC_TOKEN}" \
              -d "{
                \"correlationId\": \"$CORRELATION\",
                \"workflow\": \"consumer\",
                \"step\": 4,
                \"totalSteps\": 4,
                \"status\": \"completed\",
                \"message\": \"Page is LIVE at $DEPLOYED_URL\",
                \"deployedUrl\": \"$DEPLOYED_URL\"
              }") || HTTP_CODE="000"
            echo "📡 Final progress report: HTTP $HTTP_CODE"
            cat /tmp/curl_response.txt 2>/dev/null && echo
            if [ "$HTTP_CODE" != "200" ]; then
              echo "::warning::Final progress report failed with HTTP $HTTP_CODE"
            fi
          fi

          echo ""
          echo "🎉 ════════════════════════════════════════════"
          echo "   Page is LIVE!"
          echo "   $DEPLOYED_URL"
          echo "   ════════════════════════════════════════════"
